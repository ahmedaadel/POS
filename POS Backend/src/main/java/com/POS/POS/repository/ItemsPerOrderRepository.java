package com.POS.POS.repository;

import com.POS.POS.entity.ItemsPerOrder;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ItemsPerOrderRepository extends JpaRepository<ItemsPerOrder, Integer> {
}
